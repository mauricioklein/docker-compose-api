language: ruby

rvm: 
  - 2.2.0

sudo: required

dist: trusty

services:
  - docker

script:
  - docker info
  - docker version
  - df -kh
  - docker pull redis
  - docker run -d redis
  - docker ps
  - docker stop $(docker ps -q)
  - docker ps
